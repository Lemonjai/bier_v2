<%= form_for(winecolour) do |f| %>
  <% if winecolour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(winecolour.errors.count, "error") %> prohibited this winecolour from being saved:</h2>

      <ul>
      <% winecolour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :hexcode %>
    <%= f.text_field :hexcode %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
